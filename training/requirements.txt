# Pipeline d'entraînement Nephélé — Phase 1 (SFT) et Phase 2 (DPO).
# Cible : Apple Silicon (MPS). PAS de bitsandbytes — incompatible MPS, et inutile
# avec 64 Go de RAM unifiée (chargement 16 bits natif). Versions plancher connues
# pour supporter Qwen2.5-VL ; épingle-les exactement une fois qu'un run tourne.
torch>=2.4
transformers>=4.49.0
trl>=0.12.0
peft>=0.13.0
accelerate>=1.0.0
datasets>=3.0.0
qwen-vl-utils>=0.0.8
pillow
